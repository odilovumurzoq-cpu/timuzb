{
  "name": "timproduction-crm",
  "version": "1.0.0",
  "description": "CRM system for TimProduction",
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
